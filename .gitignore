node_modules/
dist/
*.tsbuildinfo
*.zip
.env
.env.local
.DS_Store

# Local raw capture vault (never commit raw conversations by default)
.engram/raw/

# Keep generated KB notes (private conversation summaries) out of the code repo
# — they belong in your separate private KB repo. Project docs (README,
# SECURITY, CONTRIBUTING, …) are fine to commit.
/kb/
