node_modules/
dist/
*.tsbuildinfo
.env
.env.local
.DS_Store

# Local raw capture vault (never commit raw conversations by default)
.engram/raw/

# Keep all markdown out of this code repo (KB entries are private conversation
# summaries and belong in your separate private KB repo). README is the
# one exception so the project stays documented.
*.md
!README.md
