# Screenshots

These images are referenced from the top-level `README.md`. Save them here with
these exact names (PNG):

| File | What it shows |
| --- | --- |
| `popup.png` | The toolbar popup — list of captured conversations, Export JSON / Clear. |
| `settings-providers.png` | Options page — primary provider + per-provider API keys (and the Ollama note). |
| `settings-github.png` | Options page — model override + the optional GitHub token/repo/branch. |

To regenerate: load the unpacked extension, open the popup and the Options page,
and screenshot each. A short demo `demo.gif` (capture → note appears) can go here
too and be linked from the README.
